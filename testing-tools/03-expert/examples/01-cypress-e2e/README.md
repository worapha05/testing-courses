# 01 — Cypress E2E Basics

แอป demo เล็ก ๆ (Vite + React Router) พร้อมเทส journey และ route guard

```bash
# terminal 1 — จาก folder นี้หรือชี้ root ตาม config
npx vite --config vite.config.ts

# terminal 2
npx cypress open --config-file 03-expert/examples/01-cypress-e2e/cypress.config.ts
```
