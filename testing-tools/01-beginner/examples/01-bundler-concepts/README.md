# 01 — Bundler Concepts (Dependency Graph จำลอง)

script นี้จำลองสิ่งที่ bundler ทำเมื่อเดินตาม `import` จาก entry
ไม่ใช่ bundler จริง — เพื่อให้เห็นภาพ **graph → modules → emit order**

```bash
node 01-beginner/examples/01-bundler-concepts/dependency-graph.js
```
